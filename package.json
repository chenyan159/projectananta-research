{
  "name": "investment-research-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:data": "node etl/build-data.mjs",
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "npm run build:data && vite build",
    "pages:prepare": "npm run build && node etl/sync-docs.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 8080",
    "serve": "vite preview --host 127.0.0.1 --port 8080"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.6.0",
    "dompurify": "^3.2.6",
    "lucide-react": "^0.468.0",
    "marked": "^13.0.3",
    "vite": "^6.0.7",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {}
}
